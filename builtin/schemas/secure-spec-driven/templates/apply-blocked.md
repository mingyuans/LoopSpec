# Implementation Blocked

## Blocking Issues
<!-- One bullet per blocker. Each bullet becomes an entry in `blockingIssues` for the
     next attempt's `priorAttempts`, so keep each one self-contained and actionable:
     what does not work, and what about design.md/tasks.md caused it. -->
- <!-- e.g. design.md assumes gate outputs are mutable, but rollback moves them - task
         4.2 cannot be implemented as written -->

## Code Changes Already Made
<!-- CRITICAL: rollback archives artifact files only - it never reverts code. List every
     file you already added/modified/deleted, so the next round plans against the real
     state of the working tree. Write "none" only if the working tree is untouched. -->

## Progress Reached
<!-- Which tasks in tasks.md were completed and ticked before you stopped, and which
     task you were on when you hit the blocker. -->

## Suggested Direction
<!-- Optional: where the redone design/tasks should head, without prescribing the exact
     implementation. -->
