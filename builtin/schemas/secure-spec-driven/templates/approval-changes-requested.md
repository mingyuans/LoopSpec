# Human Approval: CHANGES REQUESTED

## Changes Requested
<!-- One bullet per request. Each bullet becomes an entry in `blockingIssues` for the
     next attempt's `priorAttempts`, so keep each one self-contained and actionable:
     name the capability, file, requirement or task number instead of "this"/"that". -->
- <!-- e.g. Split the CSV export capability out of specs/data-export/spec.md; it is a
         separate change -->

## Human's Words
<!-- Verbatim quote of the feedback, including qualifiers and exceptions. The
     authoritative record - state.md keeps only the distilled version plus this path. -->

## Summary Presented to the Human
<!-- The review summary you showed them, so the next round can see what they were
     reacting to. -->

## Suggested Direction
<!-- Optional: where the redone specs/design should head, without prescribing the
     exact implementation. -->

## state.md Write-Back
<!-- Which state.md sections you appended to, and the round number. Leaving this
     empty means the feedback was never recorded in the change's memory. -->
- Decision Log: round <N> - changes requested
- Rejected Options: <!-- approaches the human ruled out -->
- Open Questions: <!-- raised but unsettled -->
- Current Focus: redo specs/design per round <N> feedback
- Artifact Notes: approval/changes-requested.md - changes requested
